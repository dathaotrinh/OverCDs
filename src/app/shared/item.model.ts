export class Item {
  constructor(public name: string, public id:string, public image: string, public type: string, public userID: string, public isDone: boolean, public username: string, public description: string) {
  }
}
